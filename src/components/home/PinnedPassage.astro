---
// Pinned scroll passage. Server-rendered verbatim (AEO / crawler-readable).
// Phrases activate in reading order as the section is scroll-pinned, filling
// from muted ink to lime as you scroll. CLS-safe: constant weight + size; only
// color animates. No-JS + reduced-motion render every phrase already activated.
---

<section class="pinned" data-pinned aria-label="What KP Solutions does">
  <div class="pinned-content">
    <p class="pinned-passage">
      Established operators use KP Solutions to <span class="activate" data-activate-at="0.10">replace
      off-the-shelf SaaS</span> with <span class="activate" data-activate-at="0.25">software
      that fits how they actually work</span> — custom platforms, internal tools, integrations, and
      <span class="activate" data-activate-at="0.45">AI-visible web presence</span>, shipped on a
      <span class="activate" data-activate-at="0.65">fixed timeline</span>, with the source code
      <span class="activate" data-activate-at="0.85">owned by you from day one</span>.
    </p>
  </div>
</section>
